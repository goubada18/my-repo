fix: 单发武器连续射击改为中断式（用户反馈：中断动画而非加速动画）

- 撤销上一轮的射击动画加速方案（speed_scale 压缩）
- fp_viewmodel_player 新增 is_shoot_locked()：射击动画播放中且进度未达
  fire_rate 间隔时锁定；超过间隔提前解锁（此时动画仍在播回稳段），
  玩家再次点击 = trigger_shoot 的 stop+play 硬中断上一发动画、从起手帧重播
- player 射击锁改用 is_shoot_locked()（原 is_shoot 全长锁）
- 3P 侧本就是包络重启的中断语义（时长已=fire_rate），两人称节奏一致
- 动画比间隔短的武器自然退化为"播完才能再射"
- probe_fixes4 ② 更新为中断语义断言：速度恒 1.0 / 击发即锁 /
  0.23s 解锁时动画仍在播（实测通过）
