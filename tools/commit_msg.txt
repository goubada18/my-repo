refactor: 武器判定数据驱动化 + 封装反射访问 + 能力框架虚方法(审查报告 C2/C5/C10)

- weapon_def.gd: 新增 weapon_type(rifle/pistol/sniper/knife/grenade)、
  fire_mode(auto/single)、scopable 三个数据字段
- player.gd: 12 处 id 字符串硬编码分支全部改读 WeaponDef 字段
  (姿态覆盖选择/内嵌枪复用/连发锁/手雷手势/开镜准入)，
  加新连发枪/新狙击枪不再被 id 判错，SCOPE_WEAPON_ID 常量删除
- 5 个武器 tres: 补配 weapon_type/fire_mode/scopable(ak47=auto，m82a1=scopable)
- ability.gd: 新增 tick(delta) 虚方法；sprint_burst 改写为 override；
  player._update_abilities 消除 "is SprintBurst" 类型特判
- player.gd: 新增 is_in_one_shot_override()/set_ability_speed_mult() 公共访问器；
  sprint_burst 弃用字符串反射 player.get/set(私有成员改名后 get 静默返回 null)
- fp_viewmodel_player.gd: 新增 get_model()/get_shoot_sfx_player()/dispose() 公共 API；
  player 侧弃用 _fp_vm.get("_model")/_model 直取/.free() 直调
