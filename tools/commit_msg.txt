feat: SWAT 动画库同步/校验工具(换皮不换骨数据链固化)

新增 tools/sync_swat_anim_lib.gd(headless)：
- 校准：对两库 21 个共有动画的每对同名轨道，从数据直接解出变换
  (位置=帧旋转+均匀缩放；旋转=恒等/前乘/后乘/共轭四模型择优)，
  用全部关键帧验证自洽性，无全局模型假设
- 校验模式(默认)：逐动画报告可同步性+失败轨道明细
- 再生成模式(--write)：旋转层全同步(两库同源实测) + 恒定比例位置轨道
  按变换同步 + 独立位置数据保留 SWAT 原值；写前自动备份 .bak；
  保留 imported 标志与 SWAT 独有动画；飞虎库只读
- 自测：对当前库执行 --write，git diff 仅剩 float32 文本噪声与
  imported 标志零差异(内容保持验证通过)，随后已还原工作区

新增 tools/probe_lib_pair.gd：两库逐动画关系指纹诊断(本次调查工具，保留)

实测结论(写入工具文档)：旋转层两库同源(恒等/Hips前乘90°X，651/651轨道
全键拟合通过)可机械同步；位置层为 SWAT 按自身体型的独立导出(逐骨缩放比
0.0098~0.0261 + 约31轨道/动画的时变真差异)；Rifle Aiming Idle 两库完全
相同=08-28 跨库复制事故的量化确认。

使用约定：修复飞虎动画后运行 --write 再生成 SWAT 库，编辑器实机目检后提交。
