fix: 切尼泊尔崩溃(locked object) + 恢复被误删的 M82 视图模型贴图

- fp_viewmodel_player.gd: dispose() 不再 free 自身——对象正在执行自己的方法时
  处于锁定状态，自释放触发 "Attempted to free a locked object (calling or
  emitting)"，切尼泊尔/沙漠之鹰/M82 等带专属 FP 场景的武器必现。
  自身改由调用方(player._rebuild_fp_viewmodel)在 dispose() 返回后立即 free。
- player.gd: _rebuild_fp_viewmodel 恢复 _fp_vm.dispose() + _fp_vm.free() 两步式，
  保留"立即释放防快速连切堆积双枪"的原语义。
- 恢复 fp_viewmodel/textures/King.png / King-2.png(含 .import)：
  二者被 m82a1_viewmodel.gltf 以 glTF 纹理 URI 引用，此前清理时纯文本扫描
  只覆盖 .gd/.tscn/.tres 未覆盖 .gltf 内部引用，误删导致 M82 第一人称
  视图模型整体加载失败(Failed loading resource)。
- tools/smoke_multichar.gd: 新增武器切换回归探针——模拟数字键直选
  尼泊尔/AK47/M82/尼泊尔 全链路(含 viewmodel 重建与释放)，永久覆盖
  本次两类事故；新增 tools/check_gltf_refs.ps1 扫描全部 gltf 外部
  纹理引用完整性(防再次误删)。
