# 2026-08-24 尼泊尔 3P 动画（用户手调 Blender → 我导出合成）

## 背景
- 用户在 Blender 里用「手枪待机 fbx 源 + FP 尼泊尔动画」导入，移出手枪待机，自己对照 FP 手动做了**轻击 + 重击**两个 NLA 非线性动画，存于桌面 `合成.blend`（Armature.001 = 3P Mixamo 骨架，scale 0.01）。
- 决策确认：下半身合成源 = `Rifle Aiming Idle`（角色默认持枪待机呼吸，全角色通用）；待机 = 轻击最后一帧(18)上半身姿态；动画全角色通用（换皮不换骨）。
- **之前的尼泊尔 3p 方案（v2/v3/breathe/3p.glb 等）全部作废移除**。

## 本机工具路径
- Blender: `C:/Program Files/Blender Foundation/Blender 4.5/blender.exe`
- Godot: `C:/Users/93343/Desktop/Godot_v4.7.1-stable_win64_console.exe`
- 命令行驱动 Blender 无需手动操作（用户多次抱怨手动跑脚本麻烦）。

## 流程（已验证可执行）
1. **Blender 导出**：`tools/export_nepal_from_blend.py` 用命令行加载 `合成.blend`，把 NLA「轻击」(0-18帧)/「重击」(0-44帧) 烘焙为独立 action，导出 glb（含 SWAT 网格 + 骨架 + 动画）。待机 = 轻击末帧静态。
   - 坑：Blender 4.5 `fcurves.new(dp, index=ai)` 必须关键字参数；`bpy.ops.export_scene.gltf` 只保留 `export_animations/export_animation_mode='ACTIONS'/export_skins` 才不报错。
   - 导出位置：`resources/animations/nepal_new/{nepal_idle,nepal_light,nepal_heavy}.glb`（每个含全部动画，冗余）。
2. **Godot 导入**：`godot --headless --import` 生成 .import。
3. **上半身裁剪**：`tools/build_nepal_upper_anims.gd` 从 glb 提取 `nepal_*` 动画，删掉下半身骨轨道（Hips+双腿+脚），存 `*_upper.tres`（89 轨）。
   - 注意 track 路径：Blender glb → `Armature_001/Skeleton3D:mixamorig_Hips`（下划线）；项目 lib → `Armature/Skeleton3D:mixamorig_Hips`。
4. **下半身合成**：`tools/build_nepal_final_anims.gd` 从 `mixamo_lib.tres` 的 `Rifle Aiming Idle` 提取下半身 19 轨，路径前缀 `Armature/`→`Armature_001/` 改写，按时间取模采样附加到 upper，生成 `*_final.tres`（108 轨）。
5. **预览场景**：`scenes/nepal_3p_preview.tscn` + `nepal_3p_preview.gd`，instance glb，控制器 _ready 时把 `*_final.tres` 注入 AnimationPlayer 并播 idle。运行时按 1/2/3 切 idle/light/heavy。**不实装到 player.gd**。
6. **清理旧方案**：39 个旧文件备份到 `.workbuddy/trash_nepal_old/`（未删，可恢复）；`mixamo_lib_swat.tres` 里旧 nepal 条目已用 Godot 脚本移除（无断裂）。

## 关键产物
- `resources/animations/nepal_new/` 目录：glb + *_upper.tres + *_final.tres
- `scenes/nepal_3p_preview.tscn` / `nepal_3p_preview.gd`（预览，未实装）

## 待办（用户确认预览效果后才做）
- 实装：把 `*_final` 动画接入 `mixamo_lib_swat.tres`（或武器 def 的动画映射），接到 player.gd 状态机。
- 命名对接：用户旧 FP 映射 `cidao1/stab` 等，需确认 3P 动画名与武器 def `fp_anim_map` 对齐。

## 尼泊尔3P试错产物回退（2026-08-24）
- 用户要求恢复到"手枪3P已解决、尼泊尔3P仍沿用步枪身体姿态、各武器FP已就绪"的状态。
- git 仓库已损坏（pack 文件 bad，refs 缺失，不可用）。项目记忆 MEMORY.md 停在 8/16 M82 最终修复，即"那句话之前"基准。
- 执行：将尼泊尔3P动画方案试错产物（nepal_new/、nepal_3p_output/、scenes/nepal_3p_*、scripts/nepal_3p_*、tools/*nepal*/*build_nepal*/*export_nepal* 等共 124 文件）移至 `.workbuddy/trash_nepal_v2/` 备份（非删除，可找回）。
- 保留项（属"武器FP/武器定义"范畴，符合用户"所有武器FP都弄好了"）：
  - `fp_viewmodel/nepal/`（尼泊尔刀 FP 视图模型 gltf/bin/png）
  - `fp_viewmodel/fp_view_config_nepal.tres`
  - `resources/weapons/nepal_kukri.tres`（武器定义）
  - character_registry/feihu/swat 中的武器引用
  - fp_viewmodel_player.gd / weapon_def.gd 内尼泊尔 FP 配置注释
- 现状：`player.gd` 无 nepal 引用；尼泊尔3P 暂无任何独立动画，沿用步枪3P身体姿态 + 刀绑手上。
- 后续（若重做尼泊尔3P）：从 FP 手臂轨迹数据出发，复用步枪3P手臂姿态方案。

## 尼泊尔FP白模 + 3P攻击卡住修复（2026-08-24 续）
- **白模根因**：`fp_viewmodel/nepal/nepal_kukri_viewmodel.gltf` 的 images URI 多写一位数字（`v_hands22`/`arm22`/`s_Light222`/`KNIFE_BORNBEAST_*22`），与实际 `textures/*.png`（末尾 `2`）不符 → 纹理加载失败 → 白模。已修正 URI 并删 `.godot/imported/nepal_kukri_viewmodel*` 缓存强制重导入。
- **3P 卡住根因**：上一 session 在 `player.gd` 埋了完整「尼泊尔 3P 复刻」`_apply_nepal_stance` 逻辑，引用 `nepal_idle_breath.tres`/`nepal_light_v3.tres`/`nepal_heavy_v3.t
res` 三文件——这三个文件从未真正落地（只在更早 trash_nepal_old），`load` 返回 null → 切换尼泊尔时卡死。
- **修复**：移除 player.gd 全部尼泊尔 3P 复刻代码（枚举/ANIM_NAMES/ONE_SHOT/常量/变量/_apply_nepal_stance/_nepal_attack_follow_lower/_nepal_lower_state_now/_nepal_combine_lower/_install_nepal_attack 及所有调用点）。保留：武器枚举 `3: "nepal_kukri"`、刀 BoneAttachment 挂右手（NEPAL_KNIFE_*、_nepal_knife/_nepal_knife_attach）。
- **行为**：尼泊尔 3P 回到「沿用步枪身体姿态 + 刀绑手上」，轻击/重击左键/右键落入通用 can_bayonet 分支（FP trigger_bayonet / 3P trigger_bayonet_shadow），不再卡。编译校验 COMPILE_OK。
- 结论：用户最初就是要「尼泊尔 3P 沿用步枪姿态」，后来加的复刻逻辑是卡顿元凶；移除后符合预期。

## 复查 + 试错文件移出项目（2026-08-24 17:5x）
- **复查结论**：player.gd 尼泊尔 3P 复刻逻辑已彻底移除，编译 COMPILE_OK。全仓二次 grep 确认无 `nepal_idle_breath/nepal_light_v3/nepal_heavy_v3/_apply_nepal_stance/_install_nepal_attack/_nepal_combine_lower/_nepal_attack_follow_lower/_nepal_lower_state_now` 残留。切换武器分支(687-695)只剩 `_want_pistol/_apply_pistol_stance`；轻击/重击无 `_is_nepal` 提前 return，落到通用步枪分支。
- **漏改修正**：发现 6 个 tools/probe_*.gd 仍调用已删除的 `_apply_nepal_stance`/`nepal_new` 路径（必然失效）→ 移出项目。
- **试错文件移出项目**（移到 `C:/Users/93343/Desktop/nepal_trial_backup_2026-08-24/`，非删除）：
  - `trash_nepal_v2/`(124) + `trash_nepal_old/`(39) 从 `.workbuddy/` 移出
  - 6 个失效 probe → `tools_probes/`
  - 根目录调试日志 13 个(_ak_log.txt 等) + user_blend_analysis.txt/user_blend_pose_analysis.txt → `root_debug_logs/`
- **保留项**：`tools/` 里 44 个"含 nepal_kukri 武器名但属通用武器/FP 测试"的 probe（无引用已删符号，无害，可正常运行）；`blender_mcp/` 19 个含 nepal 的 py（开发工具集，不进构建）；尼泊尔 FP 视图模型 + 武器定义 + 刀右手挂点。
- **现状**：项目根目录与 `.workbuddy/` 均无尼泊尔试错残留；尼泊尔 3P 沿用步枪姿态 + 刀绑手上，FP 白模已修。

## 尼泊尔3P动画重建（2026-08-24 21:xx）—— 用户自制挥砍 + 步枪身体合成
- **源**：桌面"新导出/轻击.fbx(1-19帧) 重击.fbx(1-45帧) 尼泊尔.glb(纯刀)"。用户基于手枪待机 fbx 只做了上半身手臂（锁骨/大臂/小臂/掌骨=Shoulder/Arm/ForeArm/Hand 左右8骨），骨架名被自动改成 `Armature.001`，已用 Blender 改回 `Armature` 后导出 glb（`resources/animations/nepal3p/nepal_{light,heavy}.glb`）。
- **关键坑（Blender glb 导出只完整 bake 激活 action）**：nepal_light.glb 里"轻击"完整(12-20帧)、"重击"被简化为1帧；nepal_heavy.glb 反之。**必须轻击从 light.glb 取、重击从 heavy.glb 取**。
- **产物**（`tools/build_nepal3p_arms.gd` 生成）：
  - `nepal_idle_arms.tres` = 重击末帧 8 骨静态姿态（待机持刀，1帧/骨）
  - `nepal_light_arms.tres` = 轻击挥砍 8 骨 rotation（0.633s）
  - `nepal_heavy_arms.tres` = 重击挥砍 8 骨 rotation（1.5s）
  - 只取 rotation 跳过 position（铁律）；轨迹前缀 `Armature/Skeleton3D:mixamorig_*` 与角色骨架一致（find_bone 命中验证）。
- **player.gd 接入**（复刻手枪姿态模板，避开旧坑：load 校验非 null、写库前 stop、不每帧 install）：
  - 枚举 NEPAL_ATTACK_LIGHT/HEAVY + ANIM_NAMES + ONE_SHOT_STATES
  - `_apply_nepal_stance(active)`：常驻状态（PISTOL_STANCE_STATES）合成"重击末帧持刀手臂 + 原动画身体呼吸"
  - `_install_nepal_attack(state, arms)`：一次性攻击 = 挥砍手臂 + **_state_before_one_shot 移动状态身体**循环铺满（选项B：走路/跑步/跳跃中挥刀，腿继续走/跑/跳，只手臂挥砍）
  - 轻击/重击分支：尼泊尔武器 → FP 照旧（shoot2=midslash1 / cidao1=stab），3P 播合成动画
- **验证**：编译 COMPILE_OK；合成资源级验证（combined 的 Hips/LeftLeg rotation 与 Walking 逐值一致、0 position、8 手臂轨迹）；刀挂点逻辑保留（BoneAttachment 挂 mixamorig_RightHand）。新纯刀模型已拷入 `resources/models/nepal/nepal_knife.glb`（暂未替换 nepal_world_static.gltf，等用户确认）。
- **注意**：headless 播放级骨架采样不可靠（RendererDummy），最终效果需用户在 Godot 实机确认。

## 尼泊尔3P 两个问题修复（2026-08-24 21:1x）
- **问题1：低头抬头变左歪右歪**。根因：`_apply_torso_pitch_overlay` 判断 `if _pistol_applied.is_empty()` 决定用「双手连线×上向量」还是「固定轴 -RIGHT」。尼泊尔用独立 `_nepal_applied`（非空时 _pistol_applied 仍为空）→ 走了双手连线步枪逻辑 → 持刀双手贴近/斜置，连线叉乘轴歪斜 → 低头变左右滚转。修：判断改为 `if _pistol_applied.is_empty() and _nepal_applied.is_empty()`（注释本就写明"手枪/尼泊尔(双持持刀)用固定轴"）。
- **问题2：站蹲过渡/蹲走仍是持枪动作**。根因：`_apply_nepal_stance` 用的是 `PISTOL_STANCE_STATES`（仅 7 个常驻状态），缺 STAND_TO_CROUCH/CROUCH_TO_STAND/CROUCH_WALK_*/CROUCH_STRAFE_*。修：新增 `NEPAL_STANCE_STATES`（13 状态 = 手枪 7 个 + 过渡 2 个 + 蹲走 4 个），`_apply_nepal_stance` 改用它（手枪仍用 PISTOL_STANCE_STATES 不受影响）。
- 验证：编译 COMPILE_OK；Rifle Stand To Kneel 合成 8 手臂+13 身体、Walk Crouching Forward 8 手臂+14 身体，0 position，6 个新增动画全部在库。

## 5个问题修复（2026-08-24 21:1x）
- **手枪站蹲过渡/蹲走仍持枪**：PISTOL_STANCE_STATES 扩 7→13（加 STAND_TO_CROUCH/CROUCH_TO_STAND/CROUCH_WALK_*）。
- **挥刀时俯仰强制回正**：TORSO_PITCH_STATES 加 NEPAL_ATTACK_LIGHT/HEAVY，挥刀时持续叠加头部俯仰。
- **地上影子无挥刀动画**：FP 模式下也调 `_play_one_shot_override(NEPAL_ATTACK_*)`（3P 角色 SHADOW_ONLY 投地影子随动画挥砍）。
- **待机能持刀手臂偏低**：新增常量 NEPAL_ARM_LIFT_DEG=22.0（可在源码调整），_nepal_combine 对 Shoulder 骨左乘抬臂旋转（同 _pistol_combine _q_lift 模式，sign 统一 -1）。
- **3P 刀换用户新模型（res://resources/models/nepal/nepal_knife.glb）**：新增 _compute_local_mesh_aabb() 递归合并 MeshInstance3D 局部 AABB；挂载时动态缩放到目标长度 0.38m + 居中（抵消 glb center 偏移）。新刀局部 AABB 长轴 0.17m → scale=162，旧刀局部 0.38m → scale=72.5，两刀均显示 0.38m 实长。
- 验证：编译 COMPILE_OK；探针验证新刀 scale 计算后实际长度 0.38m；Blender 渲染俯视图证实两刀朝向一致（刀柄在右下、刀刃弧形朝左上），直接复用 NEPAL_KNIFE_ROT。

## 4个bug修复 + 刀挂载预览场景（2026-08-24 21:3x）
- **蹲下挥刀无法自动起立**：one_shot_override 分支里蹲起立处理原只在换弹状态内 → 挥刀期间 _crouch_hold 残留。修：蹲下/起立逻辑提到分支顶层（所有一次性动画生效）。
- **跳跃动画持枪（尼泊尔+手枪）**：JUMP_UP/JUMP_DOWN/JUMP_FORWARD 未进姿态状态集。修：PISTOL/NEPAL_STANCE_STATES 各 +3 跳跃状态（现各 16）。
- **挥刀减速**：one_shot 分支不更新奔跑状态（is_running 锁死）→ shift+W 挥刀时无法进入奔跑。修：抽 `_update_running_state(delta, on_floor)`，正常流程与 one_shot 分支共用。
- **下半身卡顿**：挥刀合成动画播速固定 1.0，而正常移动时腿部动画按速度调速（走路 1.5x 等）→ 腿步频与物理速度不匹配。修：`_nepal_lower_speed_ratio()` 按 _state_before_one_shot 算移动倍率，`_fill_lower_track_compressed()` 对下半身轨道关键帧时间 /time_scale 后循环铺满（攻击时长不变、腿节奏匹配）。
- **预览场景**：`scenes/nepal_knife_preview.tscn` + `scripts/nepal_knife_preview.gd`（@tool）：角色 + 右手骨骼红色标注球 + 刀；编辑器改 knife_pos/knife_rot_deg/knife_scale_mult/knife_length_m 实时预览；运行时方向键移动/Shift加速/[ ]缩放，屏幕显示右手世界位置+当前参数。刀缩放用 AABB 动态计算（同 player.gd 逻辑），飞虎队 Armature scale=0.00026 时 scale≈8581 为正确值（与 Armature 缩放抵消后显示 0.38m）。
- 编译 COMPILE_OK；预览场景 headless 实例化验证 Knife/Marker/Attach 均挂载成功。

## 第3轮修复（2026-08-24 22:1x）
- **蹲下按下/松开瞬间挥刀卡死**：挥刀打断 STAND_TO_CROUCH/CROUCH_TO_STAND 过渡 → _on_transition_done 永不触发 → is_transitioning 永久锁死。修：轻击/重击分支加 `not is_transitioning`。
- **跳跃下落卡顿（所有武器）**：跳跃相关切换（起跳↔下落↔落地）blend 用 0.35s 大混合放大姿态跳变。修：`_compute_blend_time` 对含跳跃的状态对用 0.1s 短混合。
- **空中挥刀重击多次落地**：挥刀结束恢复 JUMP_DOWN（已落地）→ 重播下落动画。修：`_restore_state_after_one_shot` 恢复目标是跳跃状态且已落地 → 直接回 IDLE_AIM。
- **蹲走挥刀减速**：`_nepal_lower_speed_ratio` 蹲走误用 DESIGN_WALK_SPEED(5.0) 基准 → 腿压慢。修：蹲走用 MAX_CROUCH_SPEED(2.2) 基准。
- **点按移动键挥刀迟钝 / 转身挥刀反应不过来**：挥刀下半身锁"挥刀那一刻状态"（刚按移动=待机、转身=旧方向）。修：`_nepal_lower_state_now(on_floor)` 按实时输入推断下半身状态（走/跑/跳/蹲/方向），`_nepal_maybe_follow_lower()` 在挥刀期间状态变化时重新合成+seek（仅状态变化时 install，规避每帧崩溃坑）；`_install_nepal_attack` 加 base_override 参数；`_reset_all_locks` 复位 `_nepal_atk_lower`。
- **预览场景持 AK 待机**：`nepal_knife_preview.gd` 新增 `_apply_knife_stance()`：隐藏 Weapon_AK47 + 合成持刀待机（Rifle Aiming Idle 身体 + 手臂=idle_arms）install 并播放。验证：AK visible=false、RifleAimingIdle=22 轨迹播放中。
- 编译：player.gd / nepal_knife_preview.gd 均 OK。

## 第4轮修复（2026-08-24 22:2x）
- **蹲下+挥刀同时按仍卡 / 过渡动画没了**：上一轮 `not is_transitioning` 静默吞挥刀。修：移除限制，改为挥刀前若在站蹲过渡中 → 立即完成过渡（补 is_crouching 对应状态），再正常挥刀。同时按 = 立即蹲下并挥刀。
- **挥刀中不能跳跃**：one_shot 分支跳过跳跃检测。修：one_shot 分支补 `_jump_ok`（jump 按下+地面+非过渡+非换弹）→ `_jump_delay_timer` 启动 + `_process_movement(delta, _jump_ok)`；跳跃物理生效后 `_nepal_maybe_follow_lower` 把下半身切 JUMP_UP/DOWN（空中挥刀腿继续跳）。
- **预览场景刀挂骨骼导致无法独立调整**：刀改为独立摆放（挂场景根，不挂 BoneAttachment）。关键坑：右手世界位置必须用 `Skeleton3D.to_global(get_bone_global_pose().origin)`（骨架空间→世界，1骨架单位≠1米）；`_apply` 在 _ready 时骨架还是 rest pose（与持刀姿态差 0.5m+）→ 加 0.25s 延迟初始化，动画应用后再摆刀。验证：刀位置-右手偏差 0.133≈|knife_pos| ✓、刀长 0.38m ✓、AK 隐藏 ✓、持刀待机 22 轨迹播放 ✓。
- 用户调整流程：预览运行时方向键（米步进）移动刀、[ ] 缩放、Q/E 旋转，刀柄对准红球后把 HUD 显示的 knife_pos（相对右手的米偏移）/knife_rot_deg/knife_scale_mult 告诉开发 → 直接写 player.gd 的 NEPAL_KNIFE_POS/ROT。

## 第5轮：预览场景刀改为场景预置节点（2026-08-24 22:3x）
- **问题**：刀由脚本运行时 instantiate() 创建，编辑器里无法直接选中拖拽。
- **修**：`nepal_knife_preview.tscn` 直接 [ext_resource nepal_knife.glb] 预置 [node name="Knife"]（父=场景根）；脚本只引用（find_child）不创建。编辑器里点场景树 Knife 或 3D 视口点刀即可选中/拖拽/缩放。脚本仅：初始化延迟 0.25s 摆一次刀到右手+knife_pos、标注球挂骨骼、持刀待机合成+AK 隐藏；之后完全交给用户（编辑器拖拽 / 运行时方向键直接改 _knife.transform）。
- 新增 @export `re_place`：勾选一次可重新把刀摆回右手+offset（改完 knife_pos 等参数后用于复位）。
- HUD：实时显示 刀偏移(相对右手,米)=直接写 NEPAL_KNIFE_POS / 刀旋转(deg) / 刀缩放(实际长度)。
- 验证：knife.parent=NepalKnifePreview（可选中）✓、偏移 (0.099,0.064,-0.060)≈knife_pos ✓、刀长 0.38m ✓、标注球挂骨骼 ✓、持刀待机 22 轨迹播放 ✓。

## 第6轮：读取用户标定值写入挂载 + 红球修复（2026-08-24 22:4x）
- **用户标定**：用户在预览场景把刀拖到"刀柄对准右手"并保存（Knife transform scale=4.5、rot 30°/-23°）。
- **关键 bug**：预览脚本延迟初始化会把刀重置回"右手+默认offset"，覆盖用户拖的值 → 探针读到 scale=2.23 而非 4.5。修：**移除自动摆刀**（刀位置完全由场景 transform 决定），re_place 开关手动复位。
- **红球不在右手**：BoneAttachment3D 在编辑器里不跟随骨骼 → 红球停在 rest 位置。修：红球直接挂场景根，脚本 `_process` 每帧 `_marker.global_position = _right_hand_world_pos()`（@tool 编辑器也跑）→ 偏差 0.05mm。
- **换算公式修正**：NEPAL_KNIFE_POS 是骨骼局部系米偏移（bt.basis 须 orthonormalized 去 Armature 缩放再求逆）；`POS = bt_rot⁻¹ × (刀origin-右手origin)`（不乘 asc）；`ROT = bt_rot⁻¹ × 刀旋转`。
- **最终挂载参数（写入 player.gd）**：
  - NEPAL_KNIFE_POS = Vector3(0.42, 0.215, -0.211)（刀中心离右手 0.515m，刀长 0.765m → 刀柄抵右手）
  - NEPAL_KNIFE_ROT = Quaternion(-0.163, -0.723, -0.395, 0.543)
  - NEPAL_KNIFE_LEN_M = 0.765（= 用户 scale 4.5 × 网格长轴 0.17）
- 验证：挂载后刀中心-右手距离 0.517 ≈ |POS| ✓；编译 PLAYER_OK/PREVIEW_OK。

## 第7轮：游戏刀位置不对 → 预览与游戏姿态不一致（2026-08-24 22:5x）
- **根因**：预览场景合成持刀待机【没有】NEPAL_ARM_LIFT_DEG=22 抬臂，而游戏 _apply_nepal_stance【有】抬臂 → 右手姿态不同（位置差 0.5m+）→ 按预览标定的刀 offset 在游戏里全偏。
- **修**：预览脚本 _apply_knife_stance 加与 player.gd 完全一致的抬臂（Shoulder 左乘 _q_lift，NEPAL_ARM_LIFT_DEG=22）；按【抬臂后右手】重新换算挂载参数：
  - NEPAL_KNIFE_POS = (0.091, 0.082, -0.167)
  - NEPAL_KNIFE_ROT = Quaternion(-0.131, -0.675, -0.268, 0.675)
- **数学验证**：游戏挂载后刀中心世界 (-0.673, 2.373, 0.717) vs 用户标定 (-0.673, 2.373, 0.717)，偏差 0.4mm ✓。
- **教训（重要）**：预览标定场景与游戏运行时的动画合成必须逐参数一致（抬臂角/合成逻辑），否则标定值全废。改任一姿态参数（如 NEPAL_ARM_LIFT_DEG）后必须重标定。

## 第8轮：尼泊尔刀"进游戏不在编辑器位置" 决定性根因 + WYSIWYG 绑定（2026-08-24 23:0x）
- **决定性根因（两个，叠加）**：
  1. **BoneAttachment3D 每帧把自身 transform 覆盖成骨骼姿态** → 写在 `ba.position`/`ba.quaternion` 上的偏移/旋转【全部失效】，刀永远落在骨骼原点、无旋转。旧代码 `ba.position = NEPAL_KNIFE_POS/_asc; ba.quaternion = NEPAL_KNIFE_ROT` 全废。
  2. **Godot 4.7 `Transform3D.inverse()` 不反转缩放**（只反旋转/平移）→ 用 `bone_world.inverse()` 算挂载会把刀世界缩放算成 0/退化。必须用 `affine_inverse()`（实测正确反缩放 = 1/0.00026=3846）。
- **修法（WYSIWYG 绑定）**：偏移/旋转写在【刀本身 inst】而非 ba；`inst.transform = ba.global.affine_inverse() * mounted_Kw`，其中 `mounted_Kw = T(bone_world.origin - 刀柄世界点) * 用户刀世界transform`（刀柄吸附到手掌骨骼原点，保留用户刀的旋转/缩放）。
- **验证（probe_bind_final）**：预览标定 → 游戏式角色同 armlift idle 挂载 → 刀世界 origin 偏差 0.0125m、scale 偏差 0.000002、旋转角偏差 0.36°。那 1.25cm 是两独立场景实例待机呼吸相位不同所致，实机同骨骼挂着会像素级一致。
- **预览场景(nepal_knife_preview.tscn)** 已 WYSIWYG：character + 持刀待机合成含 NEPAL_ARM_LIFT_DEG=22（与 _apply_nepal_stance 逐参数一致）；Knife 自由节点可拖；HandleMarker(绿球) 改 Knife 子节点（拖刀跟随）；红球(RightHandMarker) 每帧 `_skel.to_global(get_bone_global_pose().origin)` 跟随手掌（修 round6"红球不在右手"——BoneAttachment 编辑器不跟骨，改用脚本每帧摆）。
- **绑定用法**：预览 F6 → 方向键移刀/[ ]大小/QE绕Y → 把绿球拖到刀柄 → 按 B → 控制台打印 3 常量（NEPAL_KNIFE_LOCAL_POS/ROT/SCALE，骨架空间原始单位，无需 /arm_scale）→ 直接覆盖 player.gd 对应常量。
- **当前 player.gd 常量 = 默认摆位标定结果**（刀在手上、旋转=默认 -90°X 猜测）。用户若想精调：编辑器拖 Knife+绿球 → 保存 → 告诉我 → 我重跑标定（1 条探针命令）。
- ⚠️ 教训：Godot 骨骼挂点标定，偏移必须写在子节点(inst)而非 BoneAttachment；逆变换统一用 affine_inverse()。

## 第9轮：红球不在手上（2026-08-24 23:1x）
- **现象**：用户在编辑器看预览场景，红球不在手上。
- **根因**：上一轮(第8轮)红球靠脚本 `_process` 每帧 `to_global(get_bone_global_pose)` 跟随。但编辑器里 @tool 的 `_process` 不可靠（或动画未播），球停在原点(脚边)→ 看着"不在手上"。运行时已证明球精确在 RightHand 骨世界坐标(偏差0.00006)，数学没错。
- **修法**：红球改用 `BoneAttachment3D`（引擎原生"贴骨骼"机制，编辑器+运行时都跟随骨骼姿态，不依赖脚本 _process）。`RightHandMarkerAttach`(BoneAttachment, bone_name=mixamorig_RightHand) 挂到 Skeleton3D，红球(MeshInstance3D) 作为其唯一子节点、local=0 → 精确钉在手腕骨=刀柄绑定点。
- 验证(probe_marker_ba)：红球世界 (-0.53720, 2.221457, 0.677169) vs RightHand 骨世界 (-0.537203, 2.22145, 0.677155)，偏差 0.000016m ✓。
- **教训(可复用)**：在 Godot 编辑器+运行时都要"让标记/物体跟骨骼"→ 用 BoneAttachment3D，别靠 `_process` 里 `to_global` 每帧算（编辑器 `_process` 不保证跑）。本类"骨骼可视化标注"模式值得存 skill。
- 注：RightHand 骨在 Mixamo 是手腕关节，可见手掌网格在关节再往外；红球钉在手腕=绑定正确点。若用户要视觉更"入掌"，需沿手骨本地轴偏移（但受 0.00026 缩放影响，需 /asc）。

## 第9轮：红球不在手上（2026-08-24 23:1x）
- **现象**：用户在编辑器看预览场景，红球不在手上。
- **根因**：上一轮(第8轮)红球靠脚本 `_process` 每帧 `to_global(get_bone_global_pose)` 跟随。但编辑器里 @tool 的 `_process` 不可靠（或动画未播），球停在原点(脚边)→ 看着"不在手上"。运行时已证明球精确在 RightHand 骨世界坐标(偏差0.00006)，数学没错。
- **修法**：红球改用 `BoneAttachment3D`（引擎原生"贴骨骼"机制，编辑器+运行时都跟随骨骼姿态，不依赖脚本 _process）。`RightHandMarkerAttach`(BoneAttachment, bone_name=mixamorig_RightHand) 挂到 Skeleton3D，红球(MeshInstance3D) 作为其唯一子节点、local=0 → 精确钉在手腕骨=刀柄绑定点。
- 验证(probe_marker_ba)：红球世界 (-0.53720, 2.221457, 0.677169) vs RightHand 骨世界 (-0.537203, 2.22145, 0.677155)，偏差 0.000016m ✓。
- **教训(可复用)**：在 Godot 编辑器+运行时都要"让标记/物体跟骨骼"→ 用 BoneAttachment3D，别靠 `_process` 里 `to_global` 每帧算（编辑器 `_process` 不保证跑）。本类"骨骼可视化标注"模式值得存 skill。
- 注：RightHand 骨在 Mixamo 是手腕关节，可见手掌网格在关节再往外；红球钉在手腕=绑定正确点。若用户要视觉更"入掌"，需沿手骨本地轴偏移（但受 0.00026 缩放影响，需 /asc）。


## 第10轮：预览场景红球/绿球都选不中（2026-08-24 23:2x）
- **用户**：预览场景里两个球都选不中（视口点不到）。
- **根因（两个叠加）**：
  1. 绿球 HandleMarker 是 GLB 实例(Knife)的子节点 → Godot 实例保护：3D 视口点选被父级网格/实例根吞掉 → 选不中，也不能独立拖。
  2. 红球是角色实例内部 Skeleton3D 的 BoneAttachment 子节点（脚本 `_skel.add_child` 进实例内部）→ 同样受实例保护，视口选不中。
- **修法**：
  - `Knife` 由「GLB 实例」改为「普通 Node3D 包裹」，内部 `KnifeModel` 才是 glb 实例；`HandleMarker` 作普通 Node3D 子节点 → 视口/场景树均可直接选中、拖拽。Knife 原 transform 不变（KnifeModel identity 子节点，glb 导入 transform 在实例内部保留）→ 渲染与标定数学完全不变。
  - 红球用 `BoneAttachment3D` 但【脱离实例】：`RightHandAttach` 作顶层节点，`use_external_skeleton=true`、`external_skeleton=NodePath("../Character/Armature/Skeleton3D")`（NodePath 相对 BoneAttachment 自身解析，需用 `../` 上溯回场景根再下钻）、`bone_name=mixamorig_RightHand`；`RightHandMarker` 作其子节点 → 顶层、可选中、原生跟骨。
- **⚠️ Godot 4.7 BoneAttachment3D 跨实例跟骨关键坑**：
  - 属性名是 `external_skeleton` + `use_external_skeleton=true`（**不是** `skeleton_path`/`bone_idx`；`get_property_list` 可见 `external_skeleton`/`use_external_skeleton`/`bone_idx`）。
  - NodePath 相对 BoneAttachment 自身解析：顶层节点写 `Character/Armature/Skeleton3D` 会被当成 `RightHandAttach/Character/...`（不存在）→ `bone_idx=-1`、球停在原点。必须 `../Character/Armature/Skeleton3D`。
  - 直接 `parent="Character/Armature/Skeleton3D"` 写覆盖子节点会 "Parent path ... has vanished when instantiating" → 子节点丢失（不要这么做）。
- **验证(headless 实例化)**：RightHandMarker world = 骨世界 (-0.537, 2.222, 0.676)，偏差 0.0；HandleMarker / Knife(Node3D) / KnifeModel 均存在。编译+预览 OK。
- **结论**：Godot「实例保护」是选择器吞掉子节点的主因；要让标记/物体既跟骨又可选中 → BoneAttachment 必须顶层 + external_skeleton 相对路径；要让 GLB 内子节点可选中 → 用普通 Node3D 包裹 glb。


## 第10轮：预览场景红球/绿球都选不中（2026-08-24 23:2x）
- **用户**：预览场景里两个球都选不中（视口点不到）。
- **根因（两个叠加）**：
  1. 绿球 HandleMarker 是 GLB 实例(Knife)的子节点 -> Godot 实例保护：3D 视口点选被父级网格/实例根吞掉 -> 选不中，也不能独立拖。
  2. 红球是角色实例内部 Skeleton3D 的 BoneAttachment 子节点（脚本 _skel.add_child 进实例内部）-> 同样受实例保护，视口选不中。
- **修法**：
  - Knife 由「GLB 实例」改为「普通 Node3D 包裹」，内部 KnifeModel 才是 glb 实例；HandleMarker 作普通 Node3D 子节点 -> 视口/场景树均可直接选中、拖拽。Knife 原 transform 不变（KnifeModel identity 子节点，glb 导入 transform 在实例内部保留）-> 渲染与标定数学完全不变。
  - 红球用 BoneAttachment3D 但「脱离实例」：RightHandAttach 作顶层节点，use_external_skeleton=true、external_skeleton=NodePath("../Character/Armature/Skeleton3D")（NodePath 相对 BoneAttachment 自身解析，需用 ../ 上溯回场景根再下钻）、bone_name=mixamorig_RightHand；RightHandMarker 作其子节点 -> 顶层、可选中、原生跟骨。
- **Godot 4.7 BoneAttachment3D 跨实例跟骨关键坑**：
  - 属性名是 external_skeleton + use_external_skeleton=true（不是 skeleton_path/bone_idx；get_property_list 可见 external_skeleton/use_external_skeleton/bone_idx）。
  - NodePath 相对 BoneAttachment 自身解析：顶层节点写 Character/Armature/Skeleton3D 会被当成 RightHandAttach/Character/...（不存在）-> bone_idx=-1、球停原点。必须 ../Character/Armature/Skeleton3D。
  - 直接 parent="Character/Armature/Skeleton3D" 写覆盖子节点会 Parent path ... has vanished when instantiating -> 子节点丢失（不要这么做）。
- **验证(headless 实例化)**：RightHandMarker world = 骨世界 (-0.537,2.222,0.676)，偏差 0.0；HandleMarker / Knife(Node3D) / KnifeModel 均存在。编译+预览 OK。
- **结论**：Godot「实例保护」是选择器吞掉子节点的主因；要让标记既跟骨又可选中 -> BoneAttachment 必须顶层 + external_skeleton 相对路径；要让 GLB 内子节点可选中 -> 用普通 Node3D 包裹 glb。

## 第13轮：3P 刀巨大 — 真正根因（跨角色 Armature 缩放锚错）（2026-08-25 00:3x）
- **用户**：第三人称刀巨大（十几米）；此前第11轮"刀在天上"时还是正常大小 → 尺寸回退是我后续改动引入。
- **真正根因（实测复现）**：游戏真实入口 boot→main_multichar.tscn，激活角色=【新SWAT】Armature≈0.013795；而预览场景/标定用的是 character.tscn（飞虎队）Armature≈0.00026。两者差 ~53 倍。WYSIWYG 用【预览骨】算 L（scale=4.5/0.00026≈17307），再挂到【游戏骨】（SWAT 0.0138）→ 0.0138×17307=238.76 → 巨大+飘20m高（headless 实跑 main_multichar 复现 world_scale=238.76、world y=20.4）。
- **修复（player.gd _mount_nepal_knife_wysiwyg）**：锚改用【游戏侧骨骼】skel（参数传入）做 bone_world = skel.global * skel.get_bone_global_pose(gbi)，再 affine_inverse 算 L；不再用预览骨 pskel。这样任意角色 Armature 缩放下，刀世界尺寸恒=编辑器里的 4.5。
- **验证（同探针复跑）**：SWAT 下刀 world_scale=238.76 → **4.5**（与编辑器一致），world y=20.4 → **2.53**（手掌高度）✓
- **铁律**：跨实例/跨角色做"相对骨骼"数学时，affine_inverse 的锚必须是【目标实例的骨】，不是标定场景的骨；两实例 Armature 缩放不同必然爆炸（53 倍级）。
- **复现方法（以后直接用）**：探针加载 res://scenes/main_multichar.tscn（唯一带 CharacterManager 的真入口；main.tscn 无 CM → 武器系统不初始化），等帧后 player.call("_select_weapon_by_id","nepal_kukri")，等 15 帧，找 NepalKnifeBone 子节点量 global_transform.basis.get_scale()。

## 第14轮：挥刀/步枪待机异常排查（2026-08-25 12:5x）
- **用户**：①飞虎队持步枪待机手动画异常 ②抬头低头变左右歪 ③挥刀瞬间点击任何操作错乱。要求挥刀与任何动作兼容。
- **排查（headless 实跑 main_multichar + 切 feihu，全部用真实链路）**：
  - 挥刀状态机/挥刀中切枪/连击/动画槽恢复：**全部正常**（初版探针传错枚举 35/36 导致 AnimState.keys()[35] 越界崩溃 → "挥刀卡死"假象；正确值 LIGHT=32/HEAVY=33）。
  - 挥刀中切回 AK47 后手部骨骼姿态偏差 0.0002m（完全恢复）；"Rifle Aiming Idle" 动画槽 v0 与切刀前一致（无污染）。
  - InputEvent 真实事件链（数字3切刀→左键→右键→数字1切回）：状态机全程正常。
- **发现并修复的真实 bug（_ensure_3p_world_model nepal 分支）**：异步挂刀完成前（2~4帧）`_weapon_rig.skip_follow` 未同步 true → WeaponRig 仍用 AK47 握把锚点每帧覆盖手部骨骼 → 切刀后立刻挥刀/移动时手被拉回持枪姿态（视觉错乱）、双手连线被拉歪 → torso pitch 叉乘轴歪（低头抬头左右歪）。修：创建 ba 后**同步**设 `_weapon_rig.skip_follow = true`。
- **遗留待用户复测**：headless 无法验证渲染/相机层视觉。若复测仍异常，需用户提供精确操作序列。
- **探针易错点**：AnimState 枚举 NEPAL_ATTACK_LIGHT=32/HEAVY=33（不是 35/36）；GDScript 不支持函数内嵌套 func（用成员函数）；`get_bone_global_pose` 返回 Transform3D 要 .origin。

## 第15轮：挥刀与所有动作兼容 — 6 处修复 + 运行时日志（2026-08-25 13:1x）
- **用户新症状**：①蹲下按下/松开瞬间挥刀卡死 ②跳跃下落卡顿(任何武器) ③空中挥刀重击多次落地 ④蹲走挥刀变慢 ⑤点移动键同时挥刀迟钝 ⑥转向时挥刀反应不过来。并要求用日志而非探针排查。
- **修复（player.gd，全部已 headless 冒烟验证不崩）**：
  1. `_play_one_shot_override` NEPAL 分支：挥刀起手用 `_nepal_lower_state_now`(input 意图) 锁定 `_state_before_one_shot`，不再用可能滞后的 current_state → 起手帧不再被 `_nepal_maybe_follow_lower` 立刻重合成（症状⑤迟钝）。
  2. `_handle_air_animation` 开头加 `_is_in_one_shot_override` 保护：挥刀/换弹中落地不再强切 IDLE_AIM（原来会夺走挥砍动画 → one_shot 状态与动画不符 → 播完回调再恢复 = 多次落地/错乱，症状③）。
  3. `_on_animation_finished` JUMP_UP/JUMP_FORWARD/JUMP_DOWN 落地回待机 + `_restore_state_after_one_shot` 恢复地面状态，均设 `landing_cooldown_timer = LANDING_COOLDOWN`（原来不设 → _handle_air_animation 下一帧落地又切一次 IDLE → 双重切换 = 跳跃下落卡顿，症状②）。
  4. `_restore_state_after_one_shot`：空中挥刀结束仍滞空时直接恢复 JUMP_DOWN（跳过 JUMP_UP 重播，症状③落地观感）。
  5. 蹲下分支取消 one_shot 时同步 `anim_player.stop()`（原来只清标志不停动画 → 挥砍播完信号按旧 current_state 分支处理 → 与蹲过渡竞争卡死，症状①）。
  6. `_nepal_lower_speed_ratio` 蹲走/站走：起手帧 velocity 未满速 → 用 input 意图速度兜底 max(spd, 0.6*满速)（原来 clamp 到 0.3 → 腿步频骤降 = 蹲走挥刀变慢，症状④）。
- **运行时日志**：新增 `NEPAL_LOG=true` 开关（player.gd 顶部，DEBUG_MODE 旁），挥刀起手/下半身重合成/动画播完/恢复/蹲打断五处打 `[NEPAL]` 前缀日志。用户真实环境复现后发控制台日志 → 精确定位剩余问题；定位完改回 false。
- **headless 帧率陷阱**：headless 无 vsync，`await process_frame` 真实时间非 60fps（15帧≈0.1s）→ 判"动画没播完/卡死"前先算真实时间，勿按帧数估。
- **Input 仿真铁律再次确认**：`Input.is_action_just_pressed` 在探针里不可靠（蹲下打断挥刀无法 headless 模拟），此类交互只能靠真实环境日志。

## 第16轮：蹲+挥刀同按 → 相机抖/蹲姿浮空/起立失效（2026-08-25 13:2x）
- **用户新症状**：同时按蹲下+挥刀 → 相机抖一下、蹲下自动起立失效、腿弹起成蹲姿浮空一瞬间又恢复。
- **根因**：`_unhandled_input` 挥刀分支（轻击/重击）的"过渡中挥刀先完成过渡"只改 state（`_change_state(CROUCH_IDLE_AIM if is_crouching else IDLE_AIM)`），**没有真正完成过渡**：相机已 set_crouch(true) 下降、碰撞体未更新、is_crouching 永不变（_on_transition_done 被跳过）→ 挥刀瞬间播站待机动画但相机在蹲位 = "腿弹起蹲姿浮空 + 相机抖"；点按蹲的自动起立链断裂 = "起立失效"。
- **修复**：
  1. 新增 `_finish_crouch_transition_now()`：按 current_state==STAND_TO_CROUCH 判定方向，完整落定（is_crouching/碰撞体高度/视觉偏移/目标视觉Y/state），轻击+重击两个挥刀分支共用。
  2. `_restore_state_after_one_shot` 增 peek 起立：挥刀结束时若 is_crouching 且 restore=CROUCH_IDLE_AIM 且蹲键已松开 → 直接完成起立（碰撞体/视觉/相机全恢复），否则点按蹲被挥刀打断后停在蹲姿。
- **翻车教训**：Edit 多行替换时**缩进必须精确匹配原文件**（GDScript 敏感），本次轻击/重击分支的注释块被替换成错误缩进导致 Parse Error（"Expected indented block after if"），第二次修复后 FINAL_VERIFY=true。
- **验证**：7 处第15轮修复 + 本轮 2 处全部 grep 确认在文件；headless 冒烟 切刀→挥刀→恢复 全流程 FINAL_VERIFY=true。

## 第17轮：编辑器不显示外部修改 + 持刀点按蹲/跳跃卡死根因（2026-08-25 13:3x）
- **用户痛点**：①"编辑器没有文件变化"——其实文件已改（时间戳+6KB+20处标记），但 **Godot 编辑器不会自动重载外部修改的脚本**（打开的 player.gd 是旧缓冲）。必须关闭重开脚本标签页或重启 Godot；且**千万别在编辑器 Ctrl+S 保存 player.gd**（旧缓冲会覆盖磁盘修复）。已在文件头加【版本标记 v16】31 行，编辑器里能看到=已加载新版。
- **持刀特有根因（新修）**：`_nepal_combine` 把合成动画 loop_mode 一律设 LOOP_LINEAR，但 STAND_TO_CROUCH/CROUCH_TO_STAND/JUMP_* 是【一次性】状态——合成版若循环播放 → animation_finished 永不触发 → 过渡/落地状态永不完成 = 持刀点按蹲"自动起立失效"、持刀跳跃下落卡顿/多次落地（持枪走原动画无此问题）。修：`_nepal_combine(lower, state)` 传 state，一次性状态设 LOOP_NONE（_play_animation 播放时也会重设，此为兜底其它直接 play 路径如 _nepal_maybe_follow_lower 重合成）。
- **给用户的验证路径**：重启 Godot → 打开 player.gd 看到 31 行 v16 标记 → F5 跑游戏复测。NEPAL_LOG 仍开，异常时发 [NEPAL] 日志。
- **教训**：外部改脚本必须提醒用户重启编辑器/重载文件，否则用户测旧代码 → "没修复"误判 + 信任崩塌。

## 第18轮：持刀点按蹲自动起立失效 — 验证结果（2026-08-25 13:4x）
- **用户重申**：持其它武器点按蹲能完整"蹲下自动起立"，就持刀有问题。
- **headless 实测（切 feihu + 切刀后）**：
  - 持刀合成版 STAND_TO_CROUCH：loop=LOOP_NONE ✓（上轮修复生效）、len=0.8s、播放后 animation_finished 触发=1、动画播完 → 完成信号链【通】。
  - position 轨道：原动画 _nepal_saved[13] 本身 tracks=39/pos=0（Mixamo 蹲下无 Hips position，靠骨骼旋转）→ "浮空"与 position 无关，_keep_pos 保留逻辑无害。
  - 持枪39轨 vs 持刀21轨差异=手臂轨道（持刀只留 8 骨 rotation），正常。
- **新增蹲/起立过渡日志**（NEPAL_LOG）：蹲下过渡开始/过渡完成(_on_transition_done)/起立过渡开始(_start_stand_transition) 三处。用户持刀点按蹲时控制台会打：蹲下开始→过渡完成→起立开始→(起立)过渡完成。哪一环缺失=问题所在。
- **验证前提**：Godot 编辑器不自动重载外部修改的脚本 → 用户必须【重启 Godot / 关闭重开 player.gd】确认第 31 行 v16 标记，否则测的是旧代码（前两轮"没修复"极可能因此）。已在文件头注明，且警告勿 Ctrl+S 覆盖。
- 状态：PARSE_OK，9 处 NEPAL_LOG 插桩。

## 第19轮：真实日志抓到"蹲+挥刀"精确根因 + 修复（2026-08-25 13:4x）
- **用户提供真实游戏日志**（关键序列）：
  [蹲下过渡开始 oneshot=false] → [挥刀起手 base=CROUCH_IDLE_AIM crouch=true] → [下半身重合成 CROUCH→IDLE(0.144)] → [挥刀结束恢复 restore=CROUCH_IDLE_AIM crouch=false]
- **精确根因**：挥刀期间【松蹲键走即时路径（_physics_process 一次性动画分支 2307 附近，直接 is_crouching=false，无过渡）】→ is_crouching 被改成 false；挥刀结束 `_restore_state_after_one_shot` 恢复起手锁定的 CROUCH_IDLE_AIM，但我的 peek 检查用 `is_crouching and restore==CROUCH` 判定 → is_crouching 已被改 false → 漏判 → 恢复成"蹲动画+站碰撞体+相机已起立"错乱 = 用户看到的"腿弹起蹲姿浮空/自动起立失效"。
- **修复**：peek 起立判定改用【restore_state==CROUCH_IDLE_AIM】+（键松开 or !is_crouching），不依赖 is_crouching；is_crouching 仍 true 时补起立落定（碰撞体/视觉/相机）。PARSE_OK。
- **附带发现**：挥刀中按/松蹲走"即时路径"（2295-2317，一次性动画分支）→ is_crouching 直接跳变、无过渡动画 → 挥刀中重合成蹲↔站跳变（日志 CROUCH→IDLE 重合成）。这是"挥刀中蹲下腿弹一下"的来源，属挥刀中即时蹲设计，peek 修复后挥刀结束会正确恢复站姿。
- **待查**：飞虎队步枪待机手动画/抬头低头歪——用户日志未覆盖（NEPAL_LOG 只打挥刀），需用户复现时补日志或自查。

## 第20轮：系统性验证 — 6 个场景全部通过 + 2 处新修复（2026-08-25 13:5x）
- **用户崩溃**（问题反复）。本轮改为【模拟真实操作序列 + 信号日志】系统验证，不逐症状猜：
  1. 蹲下按下→挥刀→松开：蹲下打断挥刀 → 蹲下过渡(13) → 自动起立(14) 流程正常推进（无卡死）。
  2. 精确模拟"蹲中挥刀→中途松蹲→挥刀结束"：恢复=IDLE_AIM/crouch=false **PEEK_OK**（第19轮 peek 修复生效，解决用户日志 restore=CROUCH_IDLE_AIM crouch=false 错乱）。
  3. 干净 AK47 跳跃落地：state=0 正常，无重复切换（JUMP 防抖生效）。
  4. 持刀跳跃落地：正常。
  5. 空中挥刀重击落地：仅一次 finished 事件，**无多次落地**。
  6. **新发现+修复**：过渡未播完切武器 → transitioning/one_shot 残留 → 新武器下状态机锁死（跳跃无效/动画停止）。修：`_switch_to_weapon` 开头清残留（transitioning→_finish_crouch_transition_now；one_shot→停动画+清 reload）。
- **版本标记 v17**（player.gd 31 行）。PARSE_OK。
- **结论**：headless 已无法复现用户报告的任何症状；用户 13:39 日志是第18轮代码（peek 修复前）。请用户重载 v17 后按清单复测，异常发 [NEPAL]+完整控制台。

## 第21轮：公共根因 — 挥刀中"下半身跟随重合成"频繁打断挥刀动画（2026-08-25 13:5x）
- **用户反馈**：一直有版本标记但"所有问题都仍存在" → 质疑没找根因。此前 headless 验证通过但真实仍出问题 → headless 输入时序与真实差异，验证有局限。
- **重新分析用户 13:39 真实日志**：挥刀 0.633s 内 `_nepal_maybe_follow_lower` 重合成 2 次（0.011s 蹲→CROUCH、0.111s 松蹲→IDLE）。**每次重合成 = stop+install+play 挥刀动画**（_install_nepal_attack 内 stop + AnimationCombiner.install remove/add + play + seek）→ 挥刀动画被反复硬重启 = "挥刀瞬间任何操作都卡顿/错乱"的公共机制（蹲/移动/转向/跳跃都会触发 want 变化 → 重合成）。
- **修复（player.gd）**：`_nepal_maybe_follow_lower` 两道闸——
  ① 起手保护 120ms（挥刀起手不跟随，挡"挥刀瞬间按蹲/移动"的立即重合成）；
  ② 限频 80ms（两次重合成最小间隔，蹲/方向快速变化合并为一次）。
  挥刀起手 `_play_one_shot_override` 记录 `_nepal_atk_start_ms`、重置 `_nepal_last_follow_ms`。PARSE_OK。版本标记 v18。
- **待用户真实数据**：飞虎队步枪待机手动画/抬头低头歪（非挥刀路径，NEPAL_LOG 无输出）——需用户复现发完整控制台（含 WARNING/SCRIPT ERROR），headless 无法定位渲染层。
- **教训**：headless 模拟与真实输入时序（_unhandled_input vs _physics_process 帧序）不同，验证通过≠真实修复；必须以真实日志为最终依据。

## 第22轮：用户 v18 真实日志证明"蹲→起立状态机正常"，浮空是视觉（2026-08-25 14:0x）
- **用户 v18 日志**：蹲下过渡开始 → 过渡完成(STAND_TO_CROUCH held=true) → 起立过渡开始 → 过渡完成(CROUCH_TO_STAND)。**状态机完整执行，无卡死**。
- **结论**：之前"卡死"判断需修正；用户看到的"腿弹起蹲姿浮空一瞬间"= **视觉问题**：持刀合成版过渡动画（STAND_TO_CROUCH/CROUCH_TO_STAND）手臂轨道用 `_nepal_idle_arms`（站姿待机持刀，悬站姿高度）而身体在下蹲 → 0.8s 过渡期手臂悬空 = "浮空"；持枪原动画手臂跟随身体所以正常。
- **修复（player.gd _nepal_combine）**：站蹲过渡状态【不替换手臂】（保留原动画手臂跟随身体下蹲/起立），蹲定后由 CROUCH_IDLE_AIM（持刀姿态）接管；跳跃/待机/走跑仍替换持刀手臂。PARSE_OK。版本标记 v19。
- **剩余待查**：飞虎队步枪待机手动画/抬头低头歪——用户仍未提供该症状的完整控制台；挥刀+操作卡顿已由第21轮"起手保护+限频"修复，待复测。

## 第23轮：确定性根因 — torso pitch 旋转轴偏 16°（抬头低头歪 + 手动画异常）（2026-08-25 14:1x）
- **用户**：F5 运行（代码生效），v19 测后仍说"所有问题没修复"。停止追问，直接找根因。
- **决定性探针**（headless 飞虎队步枪待机实测）：`_apply_torso_pitch_overlay` 步枪分支 right_skel = d_skel.cross(skel_up)，实测 d_skel=(0.278,0.031,0.960)（双手连线非纯前向，左手握护木在右前方）→ right_skel=(-0.961,0,0.278)，**偏 16°**（带 -0.278 Z 分量）→ 低头抬头时 Spine 绕偏 16° 轴旋转 → 身体带 16° 左右滚转 = "抬头低头变左右歪" + 上半身（含手臂）跟着歪 = "手动画异常"。注释写"≈-X"是经验值，真实数据偏 16°。
- **修复**：right_skel 固定为纯骨架局部 X（±X 符号按叉乘主方向），与双手位置无关。PARSE_OK。版本标记 v20。
- **教训**：不要相信注释里的经验值（"≈骨架局部 -X"），必须实测。用户 F5 运行确认代码生效，之前"没修复"多为 headless 无法覆盖的视觉/时序，这次是 headless 能实测到的数学 bug。
- **下一步**：用户测"飞虎队步枪抬头低头"；若仍歪则继续（可能 direction 反/其它）。

## 第24轮：待机手姿势不对 — 根因=切回步枪时挥砍动画残留重播（实测闭环）（2026-08-25 14:2x）
- **用户**：v20 低头正常（轴修复生效），但"持枪待机动画被影响"→ 选项确认=【手臂/手姿势不对】。
- **探针闭环（切刀→挥刀→切回 AK47 多时间点采样）**：
  - 修复前：切回后 1帧 RH 偏差 0.79m、cur=Nepal Attack Light（挥砍动画重播）→ 手 0.5s 才回持枪位。
  - 根因A：第20轮 `_switch_to_weapon` 清残留锁【先清 one_shot】→ `_apply_nepal_stance(false)` 的 893-896（one_shot 且挥刀 → current_state 回待机）被跳过 → current_state 残留 32/33 → `_restart_stance_animation` 用挥刀状态名重播挥砍。
  - 根因B：`_compute_blend_time` 挥刀→待机长混合（0.35~0.5s 含 auto_blend_boost）→ 手慢过渡。
  - 修复：①_switch_to_weapon 清 one_shot 前先修正 current_state（NEPAL_ATTACK→IDLE/CROUCH_IDLE）；②_compute_blend_time 挥刀/持刀切换一律 0.1s 短混合。
  - 修复后：切回 1帧 RH 偏差 0.0023m、cur=Rifle Aiming Idle ✓ 全部时间点 <0.003m。
- **经验**：症状=视觉（手姿势），但根因=状态机（current_state 残留）+ 混合时长。headless 用手部骨骼位置偏差做量化对比，比纯逻辑检查可靠。版本标记 v21。
