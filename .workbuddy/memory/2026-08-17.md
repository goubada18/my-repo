# 2026-08-17

## M82 开镜画面替换 + 平滑放大过渡（1s 丝滑）

### 替换图片
- 源：桌面 `m82开镜画面新.png`（1607×1158，713KB）→ `res://ui/m82_scope.png`（覆盖）。
- 删旧 `ui/m82_scope.png.import` + `.godot/imported/*.ctex` 强制重导入。
- `godot --headless --import --quit` 4m42s 跑完（项目大），重新生成 `.import`。
- 验证：`tools/probe_scope_load.gd` → size=1607×1158 OK。

### 十字交点检测（新图）
- 旧图 hardcode `Vector2(3,60)`（来自手工分析 1682×1250）。
- 新图用 `tools/probe_scope_crosshair_v2.gd` 自动探测：
  - 算法：在镜头暗区框 `r=0.30*min(w,h)` 内做列/行均值，找**窄峰**（2~4px，十字线特征）避开**宽带**（反光）。
  - 旧图验证：探测器给出 (1,58) ≈ hardcode (3,60)，方法可靠。
  - 新图：垂直线 x=801（窄峰，附近 x=800 也高），水平线 y=612（尖锐峰；y=616+ 持续亮带是镜内反光，非十字）。
  - 几何中心 (803,579) → crosshair (801,612) → **offset = (-2, +33)**。
  - crop 验证：以几何中心裁 400×400，十字交点明显偏下；以 (801,612) 裁则居中。

### 平滑 FOV 过渡（scope_overlay.gd）
- 旧：enter/exit 直接设 `cam.fov`（瞬切）→ 用户要求"放大丝滑，1s 稳定"。
- 新：
  - enter：`_fov_anim=true`，1s ease-out cubic `e = 1-(1-p)^3`（快速推近→稳定在 4x）。PNG 前景**立即 visible=true**，FOV 在背后平滑推近 = 丝滑放大。
  - exit：0.6s ease-in cubic `e = p^3`（缓慢脱离→加速回到正常）。PNG 前景**保留到动画结束再 hidden**（避免闪烁）。
  - `_process(delta)` lerp `cam.fov`；方向由 `_scoping` 决定。
  - rapid toggle（enter 中途 exit）→ 平滑反向（验证 OK）。
- 验证 `tools/probe_scope_fov.gd`：enter 70→17.5 over 1s（曲线减速），exit 17.5→70 over 0.6s（曲线加速）+ visible=false，reverse 平滑。
- 改完全项目 `--headless --quit-after 5` boot 无 Parse/Compilation 错误。

### 清理
- 删调试 crop `tools/_crop_cross.png` `_crop_center.png`。
- 保留探针 `probe_scope_load.gd` / `probe_scope_crosshair_v2.gd` / `probe_scope_fov.gd` / `probe_scope_crop.gd` 供后续换图复用。

### 坑
- 删 `.import` 后 headless `load()` 报 "No loader found"——必须先 `godot --headless --import --quit` 重生成。
- 镜内反光（宽带亮区）会被误判为十字线——必须用**窄峰**判定，或 cross-check `crop at geometric center` 看交点是否真的偏。

## 回退（用户："回退吧，到'替换 m82 开镜画面 + 过渡放大 1s'这句话之前"）
- 目标：撤销本轮所有开镜画面/放大改动，回到该指令之前的状态。
- 判定关键：`ui/m82_scope.png.import` 仍引用 `m82_scope.png-a9132516….ctex`，与 message-50 删掉的原始 import 哈希相同 →
  说明桌面 `m82开镜画面新.png` 与原始 `ui/m82_scope.png` 像素内容**一致**（import 哈希按源内容）。"替换"实为同内容重拷。
- 真正自原始起的改动**只有两处**：
  1. `scope_overlay.gd`：加了平滑放大（1s ease-out）→ 用户"不要过渡放大"已回退为瞬时；但 offset 被改成 (-2,33)、加了 viewport resize 监听 + mipmap hack、多了 `_cam`。
  2. `ui/m82_scope.png`：message-51 给 PNG **四角填黑**（flood-fill 连通域，仅填角、保留镜头透明区）——这是用户后来不想要的多余改动。
- 回退动作：
  - `ui/m82_scope.png` ← 桌面 `m82开镜画面新.png`（未填角原始内容），删 `.import`+`a9132516.ctex` 后 `godot --headless --editor --quit` 强制重导入；验证 1607×1158、四角 alpha=0（未填角）。
  - `scope_overlay.gd` 整文件还原：offset `Vector2(3,60)`、enter/exit 瞬时设 FOV、无 `_process` 动画 / 无 `_cam` / 无 viewport resize / 无 mipmap hack（`STRETCH_KEEP_ASPECT_COVERED` 保留）。
  - 删本轮建的技能 `godot-smooth-fov-scope-zoom` + 全部临时探针（`probe_find_orig_scope`/`probe_check_recovered`/`probe_recover_orig`/`probe_revert_verify`/`_recovered_orig_scope.png` 等）。
- 验证：全项目 `--headless --quit-after 5` boot 无 Parse/Compilation 错误；纹理加载 1607×1158 四角透明。
- 结论：开镜画面回到原始（瞬时 4x、十字偏移 (3,60) 按原图校准），无过渡放大、无填角。

## 动态十字对齐 + 屏幕铺满（适配所有显示器，不改 PNG 透明通道）
- 用户要求：重新定位"屏幕中心原点"适配所有显示器，重新导入桌面 `m82开镜画面新.png`，**唯一目的**是十字交点对屏幕中心、四周铺满不留空；允许拉伸变形；**不改变 PNG 透明通道**。
- alpha 探测：图 1607×1158 是真透明 scope（不是填黑图）——4 角 alpha=0、右半大片 alpha=0、镜内半透明 (0.67-0.72)、十字线/镜框/橙点 alpha~1。ring 是大圆（中心约 (801,613)，半径 ~700），源图已纵向裁切（ring 顶/底超出图边）。
- 十字交点精确测量：`tools/probe_scope_center.gd` 在镜头暗区圆内（r=0.30·min(w,h)）做**列/行均值窄峰**检测，避开镜内反光宽带 → 竖线 x=801（宽 5px）、横线 y=613（宽 3px）。**交点 (801, 613)**。
- 关键推导（保证「十字对中心」+「图片 ⊇ 视口」+「不变形」+「不改 PNG」的最简解）：
  - 均匀缩放 s，rect=(pos, pos+(w·s, h·s))，十字对中心 → pos=(VW/2−cx·s, VH/2−cy·s)。
  - "铺满"要求 pos≤0 且 pos+w·s≥VW，y 同理 → s ≥ VW/(2cx)、VW/(2(w−cx))、VH/(2cy)、VH/(2(h−cy))，取 max。
  - s_min 下 rect 严格 ⊇ 视口 → 每个屏幕像素都映到图上（图透明处透游戏 = 设计意图，不算"空"）。
  - `STRETCH_KEEP_ASPECT_COVERED` 模式 + rect 与图同比例 → 恰好填满，viewport 裁掉超出。
- 实现 `scripts/scope_overlay.gd`：
  - 抽出**纯静态函数** `compute_centering(tex_size, crosshair_px, vp) → {pos, size, scale}`，便于无 CanvasLayer 探针测试。
  - `STRETCH_KEEP_ASPECT_COVERED`（不变形）；`crosshair_px` 暴露 `@export` 默认 (801,613)；`size_changed` 重算。
  - PNG `process/fix_alpha_border=false`（reimport 已生效），透明通道不被引擎改动。
  - 保持瞬时 FOV（无过渡放大，无 `_process` 动画）。
- 验证 `tools/probe_scope_centering.gd`：12 种分辨率（1920×1080 / 2560×1440 / 1366×768 / 3840×2160 / 3440×1440 21:9 / 5120×1440 32:9 超超宽 / 1080×1920 portrait / 800×600 / 1280×1024 / 720×720 / 1680×1050 / 640×480）——
  - 十字对中心误差 ≤ 0.0001 px（仅 32:9 大缩放 3.196 浮点 epsilon，肉眼无感）
  - rect 严格 ⊇ 视口（cover=true）
  - scale_x == scale_y（不变形）
  - 12/12 ALL PASS。
- 全项目 `--headless --quit-after 4` boot 零 Parse/Compilation 错误。
## 全量代码审查（分析报告 HTML）
- 产出：`代码审查分析报告_2026-08-17_1741.html`（20.6KB，时间戳命名）。
- 范围：scripts/ 19 个生产脚本 + ui/scope_overlay.tscn（约 5500 行）。
- 结论：P0=0；P1×2（F-01 开镜 FOV 双主人+enter 无重入守卫 / F-02 开镜退出逻辑散 7 调用点）、P2×4（F-03 crosshair_px 魔数耦合 PNG / F-04 player.gd 上帝类 / F-05 _center_dirty 切换未重置 / F-07 _scope_saved_cv_visible 写入未回读）、P3×8。
- 验证正确项：居中数学 12 分辨率探针全 PASS（误差≤0.0001px）；camera_controller 不每帧写 fov（与 scope overlay 无竞争）；DEBUG_MODE=false（发布零调试开销）；V/X 两把 AK / 影子恢复 / 关镜影子 / draw 右键锁均已落地。

## 代码审查修复（F-01 重入守卫，严守零回归）
- 只修 F-01：`scope_overlay.gd` 的 `enter()` 加 `if _scoping: return` 重入守卫。
- 动机：原 `enter()` 无守卫，任何绕过 player._enter_scope 的路径重复调 enter() 会把 `_base_fov` 写成已缩放值(17.5) → exit 恢复成缩放值 → 永久 4× 锁定。守卫为纯防御、零行为变化（re-scope 流程调 enter() 前已先 exit()，overlay._scoping=false 不会被挡）。
- 验证：`probe_scope_guard.gd` 模拟「开镜→重复 enter()→退出」：base_fov 恒=70、退出 fov 恢复 70 → PASS；全项目 boot 零 Parse/Compilation 错误。探针验证后删除。
- **F-02（退出散落 10 处）主动不做统一化**：逐点核对当前行为全部正确（每处都正确退镜），非当前 bug；且站点#7（左键射击）`if _scoping:` 块内还挂 `_scope_shot_pending/_scope_shot_cancel`，机械替换会导致「未开镜左键射击误置 pending→普通射击后自动开镜」的回归。故保持现状，符合用户「不能引发一连串逻辑崩溃」前提。
- F-05/07 不适用：当前 scope_overlay.gd 无 `_center_dirty` 字段；`_scope_saved_cv_visible` 为只写不读死变量，无害不动（避免触动最高风险的 _enter/_exit_scope 内部）。

## F-02 统一开镜出口（已落地，零回归）
- 新增 `func _cancel_scope()`（纯转发 `_exit_scope()`，自带守卫），收口全部 10 处打断开镜。
- 关键坑：站点#8（右键 toggle）原为 `if _scoping:_exit_scope() else:_enter_scope()`，若整块替换会留悬空 `else:` → 语法错误/连锁崩溃。故 #8 保留 `if/else` 仅换 `_exit_scope()->_cancel_scope()`；#7（左键射击）保留 `if` 块内 `_scope_shot_pending/_scope_shot_cancel` 不外提（防未开镜射击误开镜）。其余 8 处独立块直接替换。
- 验证：全项目 boot 零 Parse/Compilation 错误；`probe_scope_switch.gd`=RESULT PASS、`probe_scope_shadow.gd`=RESULT PASS（开镜/退出/切换/同屏/影子恢复全场景无回归）。
- 临时脚本 tools/_patch_f02.py + scripts/player_f02_patched.gd 验证后已删除。

## 代码审查剩余项修复（F-03/F-05/F-06/F-07/F-08，严守零回归）
- F-07：`player.gd` 删除死变量 `_scope_saved_cv_visible`（声明 L298 + 写入 L2810，grep 全 scripts 仅 2 处、从未回读）→ 零行为变化。
- F-05：`fp_viewmodel_player.gd` 的 `set_config_paths` 顶部加 `_center_dirty = true`（新实例默认即 true→零变化；复用同 VM 场景不同网格时强制重测几何中心）。
- F-06：`scope_overlay.gd` 加 `_exit_tree` 守卫式断开 `size_changed`（防悬挂信号）。
- F-03：持久化 `tools/probe_scope_center.gd`（窄峰法自动测十字，初版阈值 0.6 误判→改列均值亮度+暗区框 r=0.30·min 复现 (801,613)）；运行时仍以 @export crosshair_px 为权威值（不做像素自动检测避免回归）；脚本注释补 F-03/F-10 资产约束。
- F-08：`weapon_rig.gd` L77 注释 `grip_real_local` 必须武器本地系。
- 验证：全项目 `--headless --quit-after 5` 编译零错误；`probe_scope_switch.gd`=/`probe_scope_shadow.gd`=RESULT PASS（F-02 收口 + F-07 清理无回归）；`probe_scope_center.gd` 实测 CROSSHAIR=(801,613) 与 export 一致。
- 递延（违前提）：F-04 上帝类→ScopeController 重构；F-11 FP 穿地（用户接受）；F-12 queue_free 延迟（已接受）；F-09 仅英文报错无行为影响。
- 报告 `代码审查分析报告_2026-08-17_1741.html` 追加第 6 节"修复进度"表。
